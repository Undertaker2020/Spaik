# media-service configuration (copy to .env)

# Shared infra (same DB as the monolith)
POSTGRES_URI=postgresql://spaik_user:spaik_pass@localhost:5433/spaik_db

# MUST match the monolith to validate access tokens
JWT_SECRET=change_me_min_32_chars

# S3 (MinIO) — same bucket as the monolith
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=spaik_admin
S3_SECRET_ACCESS_KEY=spaik_password_123
S3_BUCKET_NAME=spaik-media

# Service
MEDIA_SERVICE_PORT=4003
GRAPHQL_PREFIX=/graphql
NODE_ENV=development
ALLOWED_ORIGIN=http://localhost:3000
