# gateway configuration (copy to .env)

# Subgraph endpoints to compose
MONOLITH_SUBGRAPH_URL=http://localhost:4000/graphql
CHAT_SUBGRAPH_URL=http://localhost:4001/graphql

# Gateway
GATEWAY_PORT=4002
GRAPHQL_PREFIX=/graphql
NODE_ENV=development
ALLOWED_ORIGIN=http://localhost:3000
