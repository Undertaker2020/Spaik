{
  "name": "gateway",
  "version": "0.0.1",
  "description": "SPAIK federation gateway (Hive Gateway): composes monolith + chat + media + stream, serves queries/mutations and federated WS subscriptions",
  "private": true,
  "scripts": {
    "compose": "mesh-compose -o supergraph.graphql",
    "start": "npm run compose && hive-gateway supergraph supergraph.graphql --port 4002",
    "start:dev": "npm run compose && hive-gateway supergraph supergraph.graphql --port 4002",
    "start:prod": "hive-gateway supergraph supergraph.graphql --port 4002"
  },
  "dependencies": {
    "@graphql-hive/gateway": "^2.8.5",
    "@graphql-mesh/compose-cli": "^1.5.33",
    "graphql": "^16.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  }
}
