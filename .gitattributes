# Generated GraphQL SDL (e.g. */src/core/graphql/schema.gql) is rewritten by the
# NestJS services on every boot. Force LF so Windows CRLF rewrites don't show up
# as phantom diffs in `git status`.
*.gql text eol=lf
*.graphql text eol=lf
